# 自研分布式倒排索引搜索引擎 —— 架构设计与实现思路

## 一、项目背景与目标

在电商/二手交易等场景下，海量商品的全文检索对性能和可扩展性提出了极高要求。本方案自研一套**分布式倒排索引搜索引擎**，核心目标：

- **高性能**：毫秒级全文检索响应，支撑日均百万级查询
- **高可用**：无单点故障，节点宕机自动切换
- **可扩展**：索引分片（Sharding）支持水平扩容，数据量增长时线性扩展
- **最终一致性**：以 MySQL 为源头，异步构建索引，保证最终一致

---

## 二、整体架构概览

```
                    ┌──────────┐
                    │  Client  │
                    └────┬─────┘
                         │
                    ┌────▼─────┐
                    │  Nginx   │  (负载均衡)
                    └────┬─────┘
                         │
              ┌──────────▼──────────┐
              │  Search Coordinator │  (搜索协调器)
              │  ┌────────────────┐ │
              │  │ REST/gRPC API  │ │
              │  │ Query Parser   │ │
              │  │ Scatter-Gather │ │
              │  │ TF-IDF Ranker  │ │
              │  │ Result Merger  │ │
              │  │ Cache Layer    │ │
              │  └────────────────┘ │
              └──┬───────┬───────┬──┘
                 │       │       │
        ┌────────▼─┐ ┌──▼──────┐ ┌▼─────────┐
        │  Index   │ │  Index  │ │  Index    │
        │  Node 0  │ │  Node 1 │ │  Node 2   │
        │ (Shard 0)│ │(Shard 1)│ │ (Shard 2) │
        │          │ │         │ │           │
        │ Primary  │ │ Primary │ │ Backup    │
        │   +      │ │         │ │           │
        │ Backup   │ │         │ │           │
        └────┬─────┘ └────┬────┘ └────┬──────┘
             │            │           │
             └────────────┼───────────┘
                          │
                    ┌─────▼──────┐
                    │   Redis    │ (元数据 / 缓存 / 心跳)
                    └────────────┘
                          │
                    ┌─────▼──────┐
                    │   MySQL    │ (源数据存储)
                    └────────────┘
```

### 核心分层

| 层级 | 组件 | 职责 |
|------|------|------|
| **接入层** | Nginx | 反向代理、负载均衡、SSL 卸载 |
| **协调层** | Search Coordinator | 查询解析、分片路由、结果聚合、相关性排序 |
| **索引层** | Index Node (多 Shard) | 持有倒排索引分片，执行检索与过滤 |
| **元数据层** | Redis | 集群状态、路由表、心跳、热数据缓存 |
| **数据层** | MySQL | 原始数据存储，索引构建的数据源头 |

---

## 三、数据分片策略（Sharding）

### 3.1 分片模型

```
数据全集
   │
   ├── Shard 0  ── Primary (Node 0) + Backup (Node 2)
   ├── Shard 1  ── Primary (Node 1) + Backup (Node 0)
   └── Shard 2  ── Primary (Node 2) + Backup (Node 1)
```

### 3.2 分片路由算法

采用**一致性哈希（Consistent Hashing）**进行数据分片：

1. 对文档 ID 计算 Hash：`hash(doc_id) → 哈希环上的位置`
2. 沿哈希环顺时针找到第一个 Index Node，即为该文档所属 Shard
3. 一致性哈希的优势：**节点增减时仅影响相邻节点数据**，最小化数据迁移量

### 3.3 读写分离

- **Primary Shard**：承担写操作（索引构建/更新）
- **Backup Shard（Replica）**：承担读操作（查询检索）
- 读写分离最大化查询吞吐，Primary 故障时 Backup 自动提升

### 3.4 文档路由字段设计

每个文档在倒排索引中的存储结构示例：

```json
{
  "doc_id": "product_10086",
  "fields": {
    "标题": "iPhone 15 Pro 256GB 黑色",
    "价格": 7999,
    "分类": "数码电子",
    "标签": ["手机", "苹果", "二手"],
    "发布时间": "2026-05-10"
  }
}
```

倒排索引按字段分别建立，支持多字段联合检索与加权排序。

---

## 四、倒排索引核心结构

### 4.1 倒排索引原理

```
┌─────────────────────────────────────────────────────┐
│                    词项词典 (Term Dictionary)         │
│  ┌──────────┬──────────────────────────────────┐    │
│  │  Term    │  Posting List Pointer             │    │
│  ├──────────┼──────────────────────────────────┤    │
│  │ "手机"   │  ──────► [doc1, doc3, doc7, ...] │    │
│  │ "苹果"   │  ──────► [doc1, doc5, doc9, ...] │    │
│  │ "二手"   │  ──────► [doc1, doc2, doc4, ...] │    │
│  │ "iPhone" │  ──────► [doc1, doc8, ...]       │    │
│  └──────────┴──────────────────────────────────┘    │
│                                                      │
│                    倒排列表 (Posting List)             │
│  ┌──────────────────────────────────────────────┐    │
│  │ doc_id │ TF   │ Positions    │ Fields        │    │
│  │────────┼──────┼──────────────┼───────────────│    │
│  │  1     │  3   │ [2, 15, 28]  │ 标题, 标签     │    │
│  │  3     │  1   │ [5]          │ 标题           │    │
│  │  7     │  2   │ [8, 20]      │ 描述, 标签     │    │
│  └──────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────┘
```

### 4.2 倒排列表核心字段

| 字段 | 说明 |
|------|------|
| **doc_id** | 文档唯一标识 |
| **TF (Term Frequency)** | 词项在该文档中的出现频率 |
| **Positions** | 词项在文档中出现的位置列表（支持短语查询） |
| **Fields** | 词项出现的字段名（支持字段限定查询） |

### 4.3 索引数据结构选型

- **词项词典**：FST（Finite State Transducer）前缀树，内存高效
- **倒排列表**：跳表（Skip List）结构，支持快速求交（AND）、求并（OR）
- **正排数据**：按 doc_id 顺序存储的列式结构，快速获取字段值用于排序/过滤

---

## 五、查询处理流程（Scatter-Gather 模式）

### 5.1 整体流程

```
Client Query: "二手 手机"
         │
    ┌────▼────────┐
    │ Coordinator │  ① 解析查询 → 分词 → ["二手", "手机"]
    │             │  ② 查询路由表 → 确定目标 Shard
    └──┬──┬──┬────┘
       │  │  │
   ┌───▼──▼──▼───┐     ③ Scatter：广播查询到所有 Index Node
   │  Index Nodes │        每个 Node 在本地分片执行检索
   └──┬──┬──┬────┘
      │  │  │
   ┌──▼──▼──▼────┐     ④ Gather：收集各节点的 Top-K 结果
   │ Coordinator │     ⑤ 全局排序(TF-IDF/BM25)
   │             │     ⑥ 合并去重，返回最终结果
   └──────┬──────┘
          │
    ┌─────▼─────┐
    │  Client   │     ⑦ 返回排序后的结果列表
    └───────────┘
```

### 5.2 查询解析与分词

- **中文分词**：基于词典的逆向最大匹配 / 或集成 jieba 分词
- **Query Rewrite**：同义词扩展（"手机" → "手机" OR "电话" OR "移动设备"）
- **字段权重**：标题匹配权重 > 描述匹配权重 > 标签匹配权重

### 5.3 相关性评分（TF-IDF / BM25）

```
Score(doc) = Σ [ TF(term, doc) × IDF(term) × FieldBoost(field) ]
            ──────────────────────────────────────────────────────
                          doc_length_norm

其中：
  TF  = 词项在文档中的出现频率
  IDF = log(总文档数 / 包含该词项的文档数)
  FieldBoost = 字段权重系数（标题×3.0, 描述×1.5, 标签×2.0）
```

### 5.4 结果合并策略

1. 各 Index Node 返回本地 **Top-K** 结果及分数
2. Coordinator 使用**归并排序（K-way Merge）** 合成全局 Top-K
3. 从正排存储中回捞文档详情字段（标题、价格、缩略图等）
4. 封装为统一响应格式返回客户端

---

## 六、索引构建（MapReduce 模式）

### 6.1 离线全量构建

采用 **MapReduce 批处理** 模式进行全量索引构建：

```
                  ┌─────────┐
                  │  MySQL  │  (源数据)
                  └────┬────┘
                       │ 数据分片导出
          ┌────────────┼────────────┐
          │            │            │
     ┌────▼────┐  ┌────▼────┐  ┌────▼────┐
     │  Map 0  │  │  Map 1  │  │  Map 2  │
     │ 分词+   │  │ 分词+   │  │ 分词+   │
     │ 倒排    │  │ 倒排    │  │ 倒排    │
     └────┬────┘  └────┬────┘  └────┬────┘
          │            │            │
          └────────────┼────────────┘
                       │
                  ┌────▼────┐
                  │ Reduce  │  按 term 合并倒排列表
                  │ Shuffle │  写入对应 Shard 的 Index Node
                  └────┬────┘
                       │
          ┌────────────┼────────────┐
          │            │            │
     ┌────▼────┐  ┌────▼────┐  ┌────▼────┐
     │ Shard 0 │  │ Shard 1 │  │ Shard 2 │
     └─────────┘  └─────────┘  └─────────┘
```

### 6.2 Map 阶段

- 输入：MySQL 导出的文档批次
- 处理：对每个文档执行分词 → 统计 TF → 记录位置 → 生成 `(term, posting_entry)` 键值对
- 输出：按 term 聚合的中间结果

### 6.3 Reduce 阶段

- Shuffle：相同 term 的 posting entries 汇聚到同一 Reducer
- 合并：生成完整的 Posting List（按 doc_id 排序，构建跳表）
- 持久化：写入目标 Shard 的本地存储（如 RocksDB / 自定义文件格式）

### 6.4 实时增量更新

- 监听 MySQL Binlog 变更（INSERT / UPDATE / DELETE）
- 变更事件推送到 Kafka 消息队列
- Index Node 消费消息，局部更新对应 Shard 的倒排索引
- **最终一致性**：增量更新延迟控制在秒级

---

## 七、高可用与容灾设计

### 7.1 故障检测（心跳机制）

```
Index Node ──(每 2s)──► Redis (Heartbeat Key, TTL 10s)
                            │
                            ▼
               Coordinator 轮询 Redis，检测心跳超时
```

- 每个 Index Node 每 2 秒向 Redis 写入心跳（`heartbeat:node_id`，TTL=10 秒）
- Coordinator 定期扫描 Redis 中的心跳 Key
- 若某节点心跳 Key 过期（10 秒未更新），标记为 **Down**

### 7.2 故障转移

当 Primary Shard 所在节点宕机时：

1. Coordinator 检测到心跳超时
2. 更新 Redis 路由表：将对应 Backup Shard **提升为 Primary**
3. 新 Primary 从 Redis 中的 **Checkpoint** 恢复最新索引状态
4. 后续查询自动路由到新 Primary
5. 原节点恢复后，以 Backup 身份重新加入集群，从新 Primary 同步数据

### 7.3 多副本复制

- 每个 Shard 至少保留 **1 Primary + 1 Backup**
- Backup 从 Primary 异步拉取索引增量（Incremental Replication）
- 复制粒度：Segment 级别，支持断点续传

---

## 八、元数据管理与路由

### 8.1 Redis 中存储的元数据

```
Redis Key                          │ 说明
───────────────────────────────────┼─────────────────────
cluster:nodes                      │ 所有 Index Node 列表
cluster:shards                     │ Shard → (Primary, Backup) 映射
cluster:shard_routing              │ 一致性哈希环信息
heartbeat:node_0                   │ 节点 0 的心跳
heartbeat:node_1                   │ 节点 1 的心跳
shard:0:checkpoint                 │ Shard 0 的最后索引 Checkpoint
cache:query:{query_hash}           │ 热门查询结果缓存
```

### 8.2 路由流程

1. Coordinator 启动时从 Redis 加载路由表到内存
2. 查询到达 → 根据 `hash(doc_id)` 确定目标 Shard → 分发请求
3. 路由表变更（扩缩容/故障转移）→ 更新 Redis → 通知所有 Coordinator 热加载

---

## 九、通信协议

### 9.1 Coordinator ↔ Index Node

采用 **gRPC（首选）/ REST** 进行通信：

```protobuf
// 检索请求
message SearchRequest {
  repeated string terms = 1;       // 分词后的查询词项
  map<string, float> boosts = 2;   // 字段权重
  int32 top_k = 3;                 // 返回结果的 Top-K
  repeated string filters = 4;     // 过滤条件
}

// 检索响应
message SearchResponse {
  repeated Hit hits = 1;
  int32 total_hits = 2;            // 命中总数
  int64 took_ms = 3;               // 耗时
}

message Hit {
  string doc_id = 1;
  double score = 2;                // 相关性分数
  map<string, string> fields = 3;  // 文档摘要字段
}
```

### 9.2 通信优化

- **连接池**：Coordinator 与各 Index Node 之间维护长连接池
- **批量请求**：支持单次查询携带多个 Shard 的请求，减少 RTT
- **Protobuf 序列化**：比 JSON 体积更小、序列化更快

---

## 十、性能优化

### 10.1 多级缓存体系

```
L1: Coordinator 本地缓存 (Caffeine)
    ├── 热门查询结果缓存（TTL 5 分钟）
    └── 路由表缓存
         │
L2: Redis 分布式缓存
    ├── 跨节点共享的查询结果缓存
    └── Shard Checkpoint / 增量数据
         │
L3: Index Node 本地存储 (RocksDB)
    └── 倒排索引 + 正排数据持久化
```

### 10.2 索引存储优化

- **跳表加速**：Posting List 内建跳表，AND/OR 操作复杂度 O(log N)
- **前缀压缩**：词项词典使用 FST 存储，相比 HashMap 节省 70%+ 内存
- **增量编码**：Posting List 中 doc_id 使用差值编码（Delta Encoding）+ VarInt 压缩

### 10.3 查询优化

- **Query Cache**：相同查询（哈希匹配）直接返回缓存结果
- **Early Termination**：Top-K 场景下，当已有 K 个结果且剩余候选分数低于阈值时提前终止
- **并行检索**：多 Shard 查询完全并行，总耗时 = max(各 Shard 检索耗时)

---

## 十一、部署架构

### 11.1 推荐配置

| 组件 | 数量 | 硬件建议 |
|------|------|----------|
| Nginx | 2+ | 2C4G |
| Search Coordinator | 2-3 | 4C8G（无状态，可水平扩展） |
| Index Node | 3+ | 8C16G + 500G SSD（有状态，数据分片） |
| Redis | 3（Sentinel） | 4C8G |
| MySQL | 1 Master + 1 Slave | 8C16G + 1T SSD |

### 11.2 容量规划

- 单 Shard 建议容量：**200 万文档** / 索引大小约 5-10 GB
- 3 Shard 集群可支撑 **600 万文档** 的全文检索
- 水平扩容：增加 Index Node + 新增 Shard，一致性哈希自动重新分配

---

## 十二、技术选型总结

| 技术点 | 选型 | 理由 |
|--------|------|------|
| 编程语言 | Java | 生态成熟、JVM 调优丰富 |
| 分词器 | 自研词典分词 / jieba | 中文分词精准可控 |
| 索引存储 | RocksDB | LSM-Tree 结构，写入吞吐高 |
| 通信框架 | gRPC + Protobuf | 高性能二进制协议 |
| 协调服务 | Redis | 轻量级心跳、路由、缓存 |
| 消息队列 | Kafka | 高吞吐、持久化、支持回溯 |
| 序列化 | Protobuf | 跨语言、紧凑高效 |
| 本地缓存 | Caffeine | JVM 内最优缓存实现 |

---

## 十三、核心优势总结

1. **自研可控**：不依赖 Elasticsearch 等外部引擎，索引结构、评分算法完全自定义
2. **Scatter-Gather 查询**：天然并行化，查询延迟不随数据量线性增长
3. **一致性哈希分片**：水平扩展时数据迁移量最小
4. **MapReduce 建索引**：离线批量构建，充分利用分布式算力
5. **Primary-Backup 高可用**：心跳检测 + 自动故障转移，服务可用性 > 99.9%
6. **多级缓存体系**：L1 本地 + L2 Redis + L3 RocksDB，逐级加速
7. **MySQL Binlog 增量同步**：索引近实时更新，最终一致性

---

## 附录：扩展方向

- **语义搜索**：集成 Embedding 向量，支持 ANN（近似最近邻）检索
- **多租户**：按租户隔离 Shard，支持 SaaS 场景
- **监控告警**：接入 Prometheus + Grafana，监控 QPS / 延迟 / 分片状态
- **A/B 实验**：支持多套 Ranking 策略在线对比
